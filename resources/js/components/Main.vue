<template>
  <div class="container">
    <div class="row row-cols-1 row-cols-md-4 g-4">
      <div class="col" v-for="(post, index) in cards.posts" :key="index">
        <div class="card">
          <img :src="'/storage/' + post.image" class="card-img-top" :alt="post.title">
          <div class="card-body">
            <h3 class="card-title">{{ post.title }}</h3>
            <h4 class="card-title">{{ post.author }}</h4>
            <p class="card-text">{{ post.content }}</p>
          </div>
          <router-link class="btn btn-info" :to="{ name: 'post', params: { id: post.id } }">View</router-link>
        </div>
      </div>
    </div>
    <div class="row mt-3 bg-light">
      <ul class="list-inline bg-light">
        <li class="list-inline-item"> <button v-if="cards.prev_page_url" class="btn btn-primary" @click="change('prev_page_url')">Prev</button></li>
        <li class="list-inline-item"> <button v-if="cards.next_page_url" class="btn btn-primary" @click="change('next_page_url')">Next</button></li>
      </ul>
    </div>
  </div>
</template>

<script>

  export default {
    name: "Main",
    props:['cards'],
    methods: {
      change(vs) {
        this.$emit('change', vs);
      }
    }
  }
</script>

<style lang="scss" scoped>
</style>